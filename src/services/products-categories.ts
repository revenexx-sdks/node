import { RevenexxException, Client, type Payload, UploadProgress } from '../client';
import type { Models } from '../models';

import { RuleMatch } from '../enums/rule-match';
import { CategoriesRuleMatch } from '../enums/categories-rule-match';
import { CategoryRuleMatch } from '../enums/category-rule-match';
import { Source } from '../enums/source';
import { ProductCategoriesSource } from '../enums/product-categories-source';

export class ProductsCategories {
    client: Client;

    constructor(client: Client) {
        this.client = client;
    }

    /**
     * One node of the category tree. `parent_id` is the structure this app navigates — null is a root — while `path` is kept only for importers that carry one and nothing here reads or writes it. A category is hand-picked or RULE-DRIVEN: a non-null `rules` selector makes every matching product a `product_categories` row with source `rule`, alongside the hand-picked ones, and `rules_computed_at` says when that last completed.
     * 
     * Every column of `categories` is an exact-match query parameter, `order` sorts by one column, and `limit`/`offset` page through `page.total`. A query key that is NOT a column is dropped rather than refused, and the `filter` object echoes the ones that were understood — that echo is the only way to tell an unfiltered answer from an empty one. It reads rows exactly as they are stored: no join is resolved, no jsonb value is unpacked.
     * 
     * Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.
     *
     * @param {number} params.limit - Page size (default 50, max 200). A larger value is clamped rather than refused.
     * @param {number} params.offset - Row offset for pagination (default 0). Page with `page.total` and `page.hasMore`.
     * @param {string} params.order - Sort by one column: 'column' | 'column.asc' | 'column.desc'. A bare column sorts ascending. Anything else is refused with 400.
     * @param {string} params.id - Exact match on `id`. The row's own id, generated by the database. A client never sends one; it reads one back and uses it in the path of every later call.
     * @param {string} params.code - Exact match on `code`. The category's stable identifier — what an import and a storefront join on, and what survives a rename of the label. Unique per tenant.
     * @param {string} params.parentId - Exact match on `parent_id`. The category this one hangs under. Null is a root of the tree. Deleting a parent lifts its children to the root rather than deleting them, so a mis-click never takes a subtree with it.
     * @param {string} params.xpath - Exact match on `path`. A materialized position in the tree, kept for importers that carry one (`tools/power_tools/cordless_drills`). Nothing in this app writes or reads it — `parent_id` is the structure this app navigates.
     * @param {number} params.position - Exact match on `position`. Order among the siblings under the same parent, ascending.
     * @param {string} params.labels - Exact match on `labels`. The category name a person sees, per language tag. The catalog reads by name, not by code — a locale left blank falls back to the next filled one. The whole jsonb document is compared, serialized as JSON — this is equality, not a path or containment query, and `null` cannot be matched this way. A value that does not parse as JSON is refused with 400 `invalid_value`, naming this filter, before the request reaches the data plane.
     * @param {string} params.values - Exact match on `values`. Whatever this catalog keeps on a category beyond the model — the keys belong to the tenant, not to this app, and nothing here reads them. The whole jsonb document is compared, serialized as JSON — this is equality, not a path or containment query, and `null` cannot be matched this way. A value that does not parse as JSON is refused with 400 `invalid_value`, naming this filter, before the request reaches the data plane.
     * @param {string} params.rules - Exact match on `rules`. The selector that makes this a RULE-DRIVEN category. Null means hand-picked. Matching products are MATERIALIZED as `product_categories` rows with source `rule`, next to the hand-picked ones a recompute never touches; `POST /products/categories/{category_id}/rules/preview` dry-runs this exact document before it is stored. Conditions address the `common` bucket of a product's values — a value held per locale or per channel has no single answer for a rule to test. The whole jsonb document is compared, serialized as JSON — this is equality, not a path or containment query, and `null` cannot be matched this way. A value that does not parse as JSON is refused with 400 `invalid_value`, naming this filter, before the request reaches the data plane.
     * @param {RuleMatch} params.ruleMatch - Exact match on `rule_match`. How the conditions combine: 'all' ANDs them (the default), 'any' ORs them. It is a column of its own rather than a key of `rules` because the compiler reads the two separately.
     * @param {string} params.rulesComputedAt - Exact match on `rules_computed_at`. When the rule last ran TO COMPLETION and its memberships were synced. Null means no pass has ever finished — a recompute is chunked, so a half-finished pass leaves this untouched.
     * @param {string} params.createdAt - Exact match on `created_at`. When the row was created. Server-set — it is not part of any request body.
     * @param {string} params.updatedAt - Exact match on `updated_at`. When the row was last written. Server-set — it is not part of any request body.
     * @throws {RevenexxException}
     * @returns {Promise<{}>}
     */
    productsCategoriesList(params?: { limit?: number, offset?: number, order?: string, id?: string, code?: string, parentId?: string, xpath?: string, position?: number, labels?: string, values?: string, rules?: string, ruleMatch?: RuleMatch, rulesComputedAt?: string, createdAt?: string, updatedAt?: string }): Promise<{}>;
    /**
     * One node of the category tree. `parent_id` is the structure this app navigates — null is a root — while `path` is kept only for importers that carry one and nothing here reads or writes it. A category is hand-picked or RULE-DRIVEN: a non-null `rules` selector makes every matching product a `product_categories` row with source `rule`, alongside the hand-picked ones, and `rules_computed_at` says when that last completed.
     * 
     * Every column of `categories` is an exact-match query parameter, `order` sorts by one column, and `limit`/`offset` page through `page.total`. A query key that is NOT a column is dropped rather than refused, and the `filter` object echoes the ones that were understood — that echo is the only way to tell an unfiltered answer from an empty one. It reads rows exactly as they are stored: no join is resolved, no jsonb value is unpacked.
     * 
     * Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.
     *
     * @param {number} limit - Page size (default 50, max 200). A larger value is clamped rather than refused.
     * @param {number} offset - Row offset for pagination (default 0). Page with `page.total` and `page.hasMore`.
     * @param {string} order - Sort by one column: 'column' | 'column.asc' | 'column.desc'. A bare column sorts ascending. Anything else is refused with 400.
     * @param {string} id - Exact match on `id`. The row's own id, generated by the database. A client never sends one; it reads one back and uses it in the path of every later call.
     * @param {string} code - Exact match on `code`. The category's stable identifier — what an import and a storefront join on, and what survives a rename of the label. Unique per tenant.
     * @param {string} parentId - Exact match on `parent_id`. The category this one hangs under. Null is a root of the tree. Deleting a parent lifts its children to the root rather than deleting them, so a mis-click never takes a subtree with it.
     * @param {string} xpath - Exact match on `path`. A materialized position in the tree, kept for importers that carry one (`tools/power_tools/cordless_drills`). Nothing in this app writes or reads it — `parent_id` is the structure this app navigates.
     * @param {number} position - Exact match on `position`. Order among the siblings under the same parent, ascending.
     * @param {string} labels - Exact match on `labels`. The category name a person sees, per language tag. The catalog reads by name, not by code — a locale left blank falls back to the next filled one. The whole jsonb document is compared, serialized as JSON — this is equality, not a path or containment query, and `null` cannot be matched this way. A value that does not parse as JSON is refused with 400 `invalid_value`, naming this filter, before the request reaches the data plane.
     * @param {string} values - Exact match on `values`. Whatever this catalog keeps on a category beyond the model — the keys belong to the tenant, not to this app, and nothing here reads them. The whole jsonb document is compared, serialized as JSON — this is equality, not a path or containment query, and `null` cannot be matched this way. A value that does not parse as JSON is refused with 400 `invalid_value`, naming this filter, before the request reaches the data plane.
     * @param {string} rules - Exact match on `rules`. The selector that makes this a RULE-DRIVEN category. Null means hand-picked. Matching products are MATERIALIZED as `product_categories` rows with source `rule`, next to the hand-picked ones a recompute never touches; `POST /products/categories/{category_id}/rules/preview` dry-runs this exact document before it is stored. Conditions address the `common` bucket of a product's values — a value held per locale or per channel has no single answer for a rule to test. The whole jsonb document is compared, serialized as JSON — this is equality, not a path or containment query, and `null` cannot be matched this way. A value that does not parse as JSON is refused with 400 `invalid_value`, naming this filter, before the request reaches the data plane.
     * @param {RuleMatch} ruleMatch - Exact match on `rule_match`. How the conditions combine: 'all' ANDs them (the default), 'any' ORs them. It is a column of its own rather than a key of `rules` because the compiler reads the two separately.
     * @param {string} rulesComputedAt - Exact match on `rules_computed_at`. When the rule last ran TO COMPLETION and its memberships were synced. Null means no pass has ever finished — a recompute is chunked, so a half-finished pass leaves this untouched.
     * @param {string} createdAt - Exact match on `created_at`. When the row was created. Server-set — it is not part of any request body.
     * @param {string} updatedAt - Exact match on `updated_at`. When the row was last written. Server-set — it is not part of any request body.
     * @throws {RevenexxException}
     * @returns {Promise<{}>}
     * @deprecated Use the object parameter style method for a better developer experience.
     */
    productsCategoriesList(limit?: number, offset?: number, order?: string, id?: string, code?: string, parentId?: string, xpath?: string, position?: number, labels?: string, values?: string, rules?: string, ruleMatch?: RuleMatch, rulesComputedAt?: string, createdAt?: string, updatedAt?: string): Promise<{}>;
    productsCategoriesList(
        paramsOrFirst?: { limit?: number, offset?: number, order?: string, id?: string, code?: string, parentId?: string, xpath?: string, position?: number, labels?: string, values?: string, rules?: string, ruleMatch?: RuleMatch, rulesComputedAt?: string, createdAt?: string, updatedAt?: string } | number,
        ...rest: [(number)?, (string)?, (string)?, (string)?, (string)?, (string)?, (number)?, (string)?, (string)?, (string)?, (RuleMatch)?, (string)?, (string)?, (string)?]    
    ): Promise<{}> {
        let params: { limit?: number, offset?: number, order?: string, id?: string, code?: string, parentId?: string, xpath?: string, position?: number, labels?: string, values?: string, rules?: string, ruleMatch?: RuleMatch, rulesComputedAt?: string, createdAt?: string, updatedAt?: string };
        
        if (!paramsOrFirst || (paramsOrFirst && typeof paramsOrFirst === 'object' && !Array.isArray(paramsOrFirst))) {
            params = (paramsOrFirst || {}) as { limit?: number, offset?: number, order?: string, id?: string, code?: string, parentId?: string, xpath?: string, position?: number, labels?: string, values?: string, rules?: string, ruleMatch?: RuleMatch, rulesComputedAt?: string, createdAt?: string, updatedAt?: string };
        } else {
            params = {
                limit: paramsOrFirst as number,
                offset: rest[0] as number,
                order: rest[1] as string,
                id: rest[2] as string,
                code: rest[3] as string,
                parentId: rest[4] as string,
                xpath: rest[5] as string,
                position: rest[6] as number,
                labels: rest[7] as string,
                values: rest[8] as string,
                rules: rest[9] as string,
                ruleMatch: rest[10] as RuleMatch,
                rulesComputedAt: rest[11] as string,
                createdAt: rest[12] as string,
                updatedAt: rest[13] as string            
            };
        }
        
        const limit = params.limit;
        const offset = params.offset;
        const order = params.order;
        const id = params.id;
        const code = params.code;
        const parentId = params.parentId;
        const xpath = params.xpath;
        const position = params.position;
        const labels = params.labels;
        const values = params.values;
        const rules = params.rules;
        const ruleMatch = params.ruleMatch;
        const rulesComputedAt = params.rulesComputedAt;
        const createdAt = params.createdAt;
        const updatedAt = params.updatedAt;


        const apiPath = '/v1/products/categories';
        const apiPayload: Payload = {};
        if (typeof limit !== 'undefined') {
            apiPayload['limit'] = limit;
        }
        if (typeof offset !== 'undefined') {
            apiPayload['offset'] = offset;
        }
        if (typeof order !== 'undefined') {
            apiPayload['order'] = order;
        }
        if (typeof id !== 'undefined') {
            apiPayload['id'] = id;
        }
        if (typeof code !== 'undefined') {
            apiPayload['code'] = code;
        }
        if (typeof parentId !== 'undefined') {
            apiPayload['parent_id'] = parentId;
        }
        if (typeof xpath !== 'undefined') {
            apiPayload['path'] = xpath;
        }
        if (typeof position !== 'undefined') {
            apiPayload['position'] = position;
        }
        if (typeof labels !== 'undefined') {
            apiPayload['labels'] = labels;
        }
        if (typeof values !== 'undefined') {
            apiPayload['values'] = values;
        }
        if (typeof rules !== 'undefined') {
            apiPayload['rules'] = rules;
        }
        if (typeof ruleMatch !== 'undefined') {
            apiPayload['rule_match'] = ruleMatch;
        }
        if (typeof rulesComputedAt !== 'undefined') {
            apiPayload['rules_computed_at'] = rulesComputedAt;
        }
        if (typeof createdAt !== 'undefined') {
            apiPayload['created_at'] = createdAt;
        }
        if (typeof updatedAt !== 'undefined') {
            apiPayload['updated_at'] = updatedAt;
        }
        const uri = new URL(this.client.config.endpoint + apiPath);

        const apiHeaders: { [header: string]: string } = {
        }

        return this.client.call(
            'get',
            uri,
            apiHeaders,
            apiPayload,
        );
    }

    /**
     * Creates one category and answers 201 with the stored row, including the id and the timestamps the database filled in — a client never sends an id, it reads one back and uses it in the path of every later call.
     * 
     * One node of the category tree. `parent_id` is the structure this app navigates — null is a root — while `path` is kept only for importers that carry one and nothing here reads or writes it. A category is hand-picked or RULE-DRIVEN: a non-null `rules` selector makes every matching product a `product_categories` row with source `rule`, alongside the hand-picked ones, and `rules_computed_at` says when that last completed.
     * 
     * `code` is the only column the database refuses the row without; everything else has a default or is nullable. A second row with the same `code` answers 409.
     *
     * @param {string} params.code - The category's stable identifier — what an import and a storefront join on, and what survives a rename of the label. Unique per tenant.
     * @param {object} params.labels - The category name a person sees, per language tag. The catalog reads by name, not by code — a locale left blank falls back to the next filled one.
     * @param {string} params.parentId - The category this one hangs under. Null is a root of the tree. Deleting a parent lifts its children to the root rather than deleting them, so a mis-click never takes a subtree with it.
     * @param {string} params.xpath - A materialized position in the tree, kept for importers that carry one (`tools/power_tools/cordless_drills`). Nothing in this app writes or reads it — `parent_id` is the structure this app navigates.
     * @param {number} params.position - Order among the siblings under the same parent, ascending.
     * @param {CategoriesRuleMatch} params.ruleMatch - How the conditions combine: 'all' ANDs them (the default), 'any' ORs them. It is a column of its own rather than a key of `rules` because the compiler reads the two separately.
     * @param {object} params.rules - The selector that makes this a RULE-DRIVEN category. Null means hand-picked. Matching products are MATERIALIZED as `product_categories` rows with source `rule`, next to the hand-picked ones a recompute never touches; `POST /products/categories/{category_id}/rules/preview` dry-runs this exact document before it is stored. Conditions address the `common` bucket of a product's values — a value held per locale or per channel has no single answer for a rule to test.
     * @param {string} params.rulesComputedAt - When the rule last ran TO COMPLETION and its memberships were synced. Null means no pass has ever finished — a recompute is chunked, so a half-finished pass leaves this untouched.
     * @param {object} params.values - Whatever this catalog keeps on a category beyond the model — the keys belong to the tenant, not to this app, and nothing here reads them.
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     */
    productsCategoriesCreate(params: { code: string, labels?: object, parentId?: string, xpath?: string, position?: number, ruleMatch?: CategoriesRuleMatch, rules?: object, rulesComputedAt?: string, values?: object }): Promise<Models.Error>;
    /**
     * Creates one category and answers 201 with the stored row, including the id and the timestamps the database filled in — a client never sends an id, it reads one back and uses it in the path of every later call.
     * 
     * One node of the category tree. `parent_id` is the structure this app navigates — null is a root — while `path` is kept only for importers that carry one and nothing here reads or writes it. A category is hand-picked or RULE-DRIVEN: a non-null `rules` selector makes every matching product a `product_categories` row with source `rule`, alongside the hand-picked ones, and `rules_computed_at` says when that last completed.
     * 
     * `code` is the only column the database refuses the row without; everything else has a default or is nullable. A second row with the same `code` answers 409.
     *
     * @param {string} code - The category's stable identifier — what an import and a storefront join on, and what survives a rename of the label. Unique per tenant.
     * @param {object} labels - The category name a person sees, per language tag. The catalog reads by name, not by code — a locale left blank falls back to the next filled one.
     * @param {string} parentId - The category this one hangs under. Null is a root of the tree. Deleting a parent lifts its children to the root rather than deleting them, so a mis-click never takes a subtree with it.
     * @param {string} xpath - A materialized position in the tree, kept for importers that carry one (`tools/power_tools/cordless_drills`). Nothing in this app writes or reads it — `parent_id` is the structure this app navigates.
     * @param {number} position - Order among the siblings under the same parent, ascending.
     * @param {CategoriesRuleMatch} ruleMatch - How the conditions combine: 'all' ANDs them (the default), 'any' ORs them. It is a column of its own rather than a key of `rules` because the compiler reads the two separately.
     * @param {object} rules - The selector that makes this a RULE-DRIVEN category. Null means hand-picked. Matching products are MATERIALIZED as `product_categories` rows with source `rule`, next to the hand-picked ones a recompute never touches; `POST /products/categories/{category_id}/rules/preview` dry-runs this exact document before it is stored. Conditions address the `common` bucket of a product's values — a value held per locale or per channel has no single answer for a rule to test.
     * @param {string} rulesComputedAt - When the rule last ran TO COMPLETION and its memberships were synced. Null means no pass has ever finished — a recompute is chunked, so a half-finished pass leaves this untouched.
     * @param {object} values - Whatever this catalog keeps on a category beyond the model — the keys belong to the tenant, not to this app, and nothing here reads them.
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     * @deprecated Use the object parameter style method for a better developer experience.
     */
    productsCategoriesCreate(code: string, labels?: object, parentId?: string, xpath?: string, position?: number, ruleMatch?: CategoriesRuleMatch, rules?: object, rulesComputedAt?: string, values?: object): Promise<Models.Error>;
    productsCategoriesCreate(
        paramsOrFirst: { code: string, labels?: object, parentId?: string, xpath?: string, position?: number, ruleMatch?: CategoriesRuleMatch, rules?: object, rulesComputedAt?: string, values?: object } | string,
        ...rest: [(object)?, (string)?, (string)?, (number)?, (CategoriesRuleMatch)?, (object)?, (string)?, (object)?]    
    ): Promise<Models.Error> {
        let params: { code: string, labels?: object, parentId?: string, xpath?: string, position?: number, ruleMatch?: CategoriesRuleMatch, rules?: object, rulesComputedAt?: string, values?: object };
        
        if ((paramsOrFirst && typeof paramsOrFirst === 'object' && !Array.isArray(paramsOrFirst))) {
            params = (paramsOrFirst || {}) as { code: string, labels?: object, parentId?: string, xpath?: string, position?: number, ruleMatch?: CategoriesRuleMatch, rules?: object, rulesComputedAt?: string, values?: object };
        } else {
            params = {
                code: paramsOrFirst as string,
                labels: rest[0] as object,
                parentId: rest[1] as string,
                xpath: rest[2] as string,
                position: rest[3] as number,
                ruleMatch: rest[4] as CategoriesRuleMatch,
                rules: rest[5] as object,
                rulesComputedAt: rest[6] as string,
                values: rest[7] as object            
            };
        }
        
        const code = params.code;
        const labels = params.labels;
        const parentId = params.parentId;
        const xpath = params.xpath;
        const position = params.position;
        const ruleMatch = params.ruleMatch;
        const rules = params.rules;
        const rulesComputedAt = params.rulesComputedAt;
        const values = params.values;

        if (typeof code === 'undefined') {
            throw new RevenexxException('Missing required parameter: "code"');
        }

        const apiPath = '/v1/products/categories';
        const apiPayload: Payload = {};
        if (typeof code !== 'undefined') {
            apiPayload['code'] = code;
        }
        if (typeof labels !== 'undefined') {
            apiPayload['labels'] = labels;
        }
        if (typeof parentId !== 'undefined') {
            apiPayload['parent_id'] = parentId;
        }
        if (typeof xpath !== 'undefined') {
            apiPayload['path'] = xpath;
        }
        if (typeof position !== 'undefined') {
            apiPayload['position'] = position;
        }
        if (typeof ruleMatch !== 'undefined') {
            apiPayload['rule_match'] = ruleMatch;
        }
        if (typeof rules !== 'undefined') {
            apiPayload['rules'] = rules;
        }
        if (typeof rulesComputedAt !== 'undefined') {
            apiPayload['rules_computed_at'] = rulesComputedAt;
        }
        if (typeof values !== 'undefined') {
            apiPayload['values'] = values;
        }
        const uri = new URL(this.client.config.endpoint + apiPath);

        const apiHeaders: { [header: string]: string } = {
            'content-type': 'application/json',
        }

        return this.client.call(
            'post',
            uri,
            apiHeaders,
            apiPayload,
        );
    }

    /**
     * What the nightly `recompute-category-rules` schedule calls, and the call to reach for after a bulk import has changed what the rules select. Same sync as the single-category recompute, applied to every category with non-null rules. The whole run shares ONE budget: a category the budget no longer reaches is reported as `skipped` and picked up by the next run, and a failing category is reported in its result entry instead of aborting the run.
     *
     * @param {object} params.data - Request body
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     */
    productsCategoriesRulesRecomputeAll(params: { data: object }): Promise<Models.Error>;
    /**
     * What the nightly `recompute-category-rules` schedule calls, and the call to reach for after a bulk import has changed what the rules select. Same sync as the single-category recompute, applied to every category with non-null rules. The whole run shares ONE budget: a category the budget no longer reaches is reported as `skipped` and picked up by the next run, and a failing category is reported in its result entry instead of aborting the run.
     *
     * @param {object} data - Request body
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     * @deprecated Use the object parameter style method for a better developer experience.
     */
    productsCategoriesRulesRecomputeAll(data: object): Promise<Models.Error>;
    productsCategoriesRulesRecomputeAll(
        paramsOrFirst: { data: object } | object    
    ): Promise<Models.Error> {
        let params: { data: object };
        
        if ((paramsOrFirst && typeof paramsOrFirst === 'object' && !Array.isArray(paramsOrFirst) && ('data' in paramsOrFirst))) {
            params = (paramsOrFirst || {}) as { data: object };
        } else {
            params = {
                data: paramsOrFirst as object            
            };
        }
        
        const data = params.data;

        if (typeof data === 'undefined') {
            throw new RevenexxException('Missing required parameter: "data"');
        }

        const apiPath = '/v1/products/categories/rules/recompute-all';
        const apiPayload: Payload = {};
        if (typeof data !== 'undefined') {
            Object.assign(apiPayload, data);
        }
        const uri = new URL(this.client.config.endpoint + apiPath);

        const apiHeaders: { [header: string]: string } = {
            'content-type': 'application/json',
        }

        return this.client.call(
            'post',
            uri,
            apiHeaders,
            apiPayload,
        );
    }

    /**
     * Dry-runs a rule: how many products it selects, plus a sample of up to ten, and it WRITES NOTHING. Evaluates the rule in the request body against the live catalog WITHOUT touching product_categories — this powers the cockpit's "matches N products" preview while an operator edits a rule. Soft-deleted products are excluded. Counting is delegated to the database, never enumerated: a rule that compiles to a single query is answered by one exact-count request whatever its match set. A rule that needs several queries (rule_match "any", or a repeated column such as a range) is combined in the app and stops at `cap` ids — check `capped` before showing `count` as a total.
     *
     * @param {string} params.categoryId - The `categories` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/categories`. An id no categorie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
     * @param {Models.CategoryRuleCondition[]} params.conditions - Between 1 and 25 conditions — a rule is a selector, not a query language. An empty list is a 400, not "everything".
     * @param {CategoryRuleMatch} params.ruleMatch - 'all' ANDs every condition (default), 'any' ORs them.
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     */
    productsCategoriesRulesPreview(params: { categoryId: string, conditions: Models.CategoryRuleCondition[], ruleMatch?: CategoryRuleMatch }): Promise<Models.Error>;
    /**
     * Dry-runs a rule: how many products it selects, plus a sample of up to ten, and it WRITES NOTHING. Evaluates the rule in the request body against the live catalog WITHOUT touching product_categories — this powers the cockpit's "matches N products" preview while an operator edits a rule. Soft-deleted products are excluded. Counting is delegated to the database, never enumerated: a rule that compiles to a single query is answered by one exact-count request whatever its match set. A rule that needs several queries (rule_match "any", or a repeated column such as a range) is combined in the app and stops at `cap` ids — check `capped` before showing `count` as a total.
     *
     * @param {string} categoryId - The `categories` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/categories`. An id no categorie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
     * @param {Models.CategoryRuleCondition[]} conditions - Between 1 and 25 conditions — a rule is a selector, not a query language. An empty list is a 400, not "everything".
     * @param {CategoryRuleMatch} ruleMatch - 'all' ANDs every condition (default), 'any' ORs them.
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     * @deprecated Use the object parameter style method for a better developer experience.
     */
    productsCategoriesRulesPreview(categoryId: string, conditions: Models.CategoryRuleCondition[], ruleMatch?: CategoryRuleMatch): Promise<Models.Error>;
    productsCategoriesRulesPreview(
        paramsOrFirst: { categoryId: string, conditions: Models.CategoryRuleCondition[], ruleMatch?: CategoryRuleMatch } | string,
        ...rest: [(Models.CategoryRuleCondition[])?, (CategoryRuleMatch)?]    
    ): Promise<Models.Error> {
        let params: { categoryId: string, conditions: Models.CategoryRuleCondition[], ruleMatch?: CategoryRuleMatch };
        
        if ((paramsOrFirst && typeof paramsOrFirst === 'object' && !Array.isArray(paramsOrFirst))) {
            params = (paramsOrFirst || {}) as { categoryId: string, conditions: Models.CategoryRuleCondition[], ruleMatch?: CategoryRuleMatch };
        } else {
            params = {
                categoryId: paramsOrFirst as string,
                conditions: rest[0] as Models.CategoryRuleCondition[],
                ruleMatch: rest[1] as CategoryRuleMatch            
            };
        }
        
        const categoryId = params.categoryId;
        const conditions = params.conditions;
        const ruleMatch = params.ruleMatch;

        if (typeof categoryId === 'undefined') {
            throw new RevenexxException('Missing required parameter: "categoryId"');
        }
        if (typeof conditions === 'undefined') {
            throw new RevenexxException('Missing required parameter: "conditions"');
        }

        const apiPath = '/v1/products/categories/{category_id}/rules/preview'.replace('{category_id}', categoryId);
        const apiPayload: Payload = {};
        if (typeof conditions !== 'undefined') {
            apiPayload['conditions'] = conditions;
        }
        if (typeof ruleMatch !== 'undefined') {
            apiPayload['rule_match'] = ruleMatch;
        }
        const uri = new URL(this.client.config.endpoint + apiPath);

        const apiHeaders: { [header: string]: string } = {
            'content-type': 'application/json',
        }

        return this.client.call(
            'post',
            uri,
            apiHeaders,
            apiPayload,
        );
    }

    /**
     * Syncs one category's rule-derived memberships to what its stored rule selects today. Evaluates categories.rules (NOT the request body), then inserts the newly matching products as source='rule' rows and deletes the rule rows that no longer match. Manual (source='manual') memberships are never inserted, deleted or shadowed. Stamps categories.rules_computed_at.
     * 
     * A large category does NOT finish in one call: the run stops when its wall-clock budget is spent and answers `done: false` with the `cursor` to send back, so drive it in a loop until `done` is true.
     *
     * @param {string} params.categoryId - The `categories` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/categories`. An id no categorie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
     * @param {string} params.cursor - The `cursor` a previous call returned, to continue that pass. Send `null` explicitly to restart from the beginning; omit the field to let the app decide (resume if a pass is in flight, otherwise start fresh). Anything that is not a string or null is a 400.
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     */
    productsCategoriesRulesRecompute(params: { categoryId: string, cursor?: string }): Promise<Models.Error>;
    /**
     * Syncs one category's rule-derived memberships to what its stored rule selects today. Evaluates categories.rules (NOT the request body), then inserts the newly matching products as source='rule' rows and deletes the rule rows that no longer match. Manual (source='manual') memberships are never inserted, deleted or shadowed. Stamps categories.rules_computed_at.
     * 
     * A large category does NOT finish in one call: the run stops when its wall-clock budget is spent and answers `done: false` with the `cursor` to send back, so drive it in a loop until `done` is true.
     *
     * @param {string} categoryId - The `categories` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/categories`. An id no categorie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
     * @param {string} cursor - The `cursor` a previous call returned, to continue that pass. Send `null` explicitly to restart from the beginning; omit the field to let the app decide (resume if a pass is in flight, otherwise start fresh). Anything that is not a string or null is a 400.
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     * @deprecated Use the object parameter style method for a better developer experience.
     */
    productsCategoriesRulesRecompute(categoryId: string, cursor?: string): Promise<Models.Error>;
    productsCategoriesRulesRecompute(
        paramsOrFirst: { categoryId: string, cursor?: string } | string,
        ...rest: [(string)?]    
    ): Promise<Models.Error> {
        let params: { categoryId: string, cursor?: string };
        
        if ((paramsOrFirst && typeof paramsOrFirst === 'object' && !Array.isArray(paramsOrFirst))) {
            params = (paramsOrFirst || {}) as { categoryId: string, cursor?: string };
        } else {
            params = {
                categoryId: paramsOrFirst as string,
                cursor: rest[0] as string            
            };
        }
        
        const categoryId = params.categoryId;
        const cursor = params.cursor;

        if (typeof categoryId === 'undefined') {
            throw new RevenexxException('Missing required parameter: "categoryId"');
        }

        const apiPath = '/v1/products/categories/{category_id}/rules/recompute'.replace('{category_id}', categoryId);
        const apiPayload: Payload = {};
        if (typeof cursor !== 'undefined') {
            apiPayload['cursor'] = cursor;
        }
        const uri = new URL(this.client.config.endpoint + apiPath);

        const apiHeaders: { [header: string]: string } = {
            'content-type': 'application/json',
        }

        return this.client.call(
            'post',
            uri,
            apiHeaders,
            apiPayload,
        );
    }

    /**
     * Deletes one category by id. It is a hard delete — the row is gone, and the answer is a confirmation rather than a result to branch on.
     * 
     * It takes what hangs off it: product category memberships (`category_id`) are deleted with it. `categories.parent_id` is set to null instead, so the rows that pointed at it survive the delete rather than going with it.
     * 
     * An id no category of this tenant carries answers 404; there is no 409, because every foreign key pointing at this entity resolves itself on delete rather than blocking one.
     *
     * @param {string} params.id - The `categories` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/categories`. An id no categorie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     */
    productsCategoriesDelete(params: { id: string }): Promise<Models.Error>;
    /**
     * Deletes one category by id. It is a hard delete — the row is gone, and the answer is a confirmation rather than a result to branch on.
     * 
     * It takes what hangs off it: product category memberships (`category_id`) are deleted with it. `categories.parent_id` is set to null instead, so the rows that pointed at it survive the delete rather than going with it.
     * 
     * An id no category of this tenant carries answers 404; there is no 409, because every foreign key pointing at this entity resolves itself on delete rather than blocking one.
     *
     * @param {string} id - The `categories` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/categories`. An id no categorie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     * @deprecated Use the object parameter style method for a better developer experience.
     */
    productsCategoriesDelete(id: string): Promise<Models.Error>;
    productsCategoriesDelete(
        paramsOrFirst: { id: string } | string    
    ): Promise<Models.Error> {
        let params: { id: string };
        
        if ((paramsOrFirst && typeof paramsOrFirst === 'object' && !Array.isArray(paramsOrFirst))) {
            params = (paramsOrFirst || {}) as { id: string };
        } else {
            params = {
                id: paramsOrFirst as string            
            };
        }
        
        const id = params.id;

        if (typeof id === 'undefined') {
            throw new RevenexxException('Missing required parameter: "id"');
        }

        const apiPath = '/v1/products/categories/{id}'.replace('{id}', id);
        const apiPayload: Payload = {};
        const uri = new URL(this.client.config.endpoint + apiPath);

        const apiHeaders: { [header: string]: string } = {
        }

        return this.client.call(
            'delete',
            uri,
            apiHeaders,
            apiPayload,
        );
    }

    /**
     * Reads one category by its id — the whole row, every column, as it is stored.
     * 
     * One node of the category tree. `parent_id` is the structure this app navigates — null is a root — while `path` is kept only for importers that carry one and nothing here reads or writes it. A category is hand-picked or RULE-DRIVEN: a non-null `rules` selector makes every matching product a `product_categories` row with source `rule`, alongside the hand-picked ones, and `rules_computed_at` says when that last completed.
     * 
     * An id no category of this tenant carries answers 404, and so does one belonging to another tenant: row-level security makes that row invisible rather than forbidden. A malformed id answers 400 before the route is reached.
     * 
     * Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.
     *
     * @param {string} params.id - The `categories` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/categories`. An id no categorie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     */
    productsCategoriesGet(params: { id: string }): Promise<Models.Error>;
    /**
     * Reads one category by its id — the whole row, every column, as it is stored.
     * 
     * One node of the category tree. `parent_id` is the structure this app navigates — null is a root — while `path` is kept only for importers that carry one and nothing here reads or writes it. A category is hand-picked or RULE-DRIVEN: a non-null `rules` selector makes every matching product a `product_categories` row with source `rule`, alongside the hand-picked ones, and `rules_computed_at` says when that last completed.
     * 
     * An id no category of this tenant carries answers 404, and so does one belonging to another tenant: row-level security makes that row invisible rather than forbidden. A malformed id answers 400 before the route is reached.
     * 
     * Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.
     *
     * @param {string} id - The `categories` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/categories`. An id no categorie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     * @deprecated Use the object parameter style method for a better developer experience.
     */
    productsCategoriesGet(id: string): Promise<Models.Error>;
    productsCategoriesGet(
        paramsOrFirst: { id: string } | string    
    ): Promise<Models.Error> {
        let params: { id: string };
        
        if ((paramsOrFirst && typeof paramsOrFirst === 'object' && !Array.isArray(paramsOrFirst))) {
            params = (paramsOrFirst || {}) as { id: string };
        } else {
            params = {
                id: paramsOrFirst as string            
            };
        }
        
        const id = params.id;

        if (typeof id === 'undefined') {
            throw new RevenexxException('Missing required parameter: "id"');
        }

        const apiPath = '/v1/products/categories/{id}'.replace('{id}', id);
        const apiPayload: Payload = {};
        const uri = new URL(this.client.config.endpoint + apiPath);

        const apiHeaders: { [header: string]: string } = {
        }

        return this.client.call(
            'get',
            uri,
            apiHeaders,
            apiPayload,
        );
    }

    /**
     * Updates one category by id. A partial patch: the body names only the columns to change and every column it leaves out keeps its current value, so there is no read-modify-write and no way to blank a field by forgetting it.
     * 
     * One node of the category tree. `parent_id` is the structure this app navigates — null is a root — while `path` is kept only for importers that carry one and nothing here reads or writes it. A category is hand-picked or RULE-DRIVEN: a non-null `rules` selector makes every matching product a `product_categories` row with source `rule`, alongside the hand-picked ones, and `rules_computed_at` says when that last completed.
     * 
     * A body that names nothing writable is refused with 400 rather than answered as a no-op, an id nobody carries answers 404, and a value that collides on `code` answers 409.
     *
     * @param {string} params.id - The `categories` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/categories`. An id no categorie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
     * @param {string} params.code - The category's stable identifier — what an import and a storefront join on, and what survives a rename of the label. Unique per tenant.
     * @param {object} params.labels - The category name a person sees, per language tag. The catalog reads by name, not by code — a locale left blank falls back to the next filled one.
     * @param {string} params.parentId - The category this one hangs under. Null is a root of the tree. Deleting a parent lifts its children to the root rather than deleting them, so a mis-click never takes a subtree with it.
     * @param {string} params.xpath - A materialized position in the tree, kept for importers that carry one (`tools/power_tools/cordless_drills`). Nothing in this app writes or reads it — `parent_id` is the structure this app navigates.
     * @param {number} params.position - Order among the siblings under the same parent, ascending.
     * @param {CategoriesRuleMatch} params.ruleMatch - How the conditions combine: 'all' ANDs them (the default), 'any' ORs them. It is a column of its own rather than a key of `rules` because the compiler reads the two separately.
     * @param {object} params.rules - The selector that makes this a RULE-DRIVEN category. Null means hand-picked. Matching products are MATERIALIZED as `product_categories` rows with source `rule`, next to the hand-picked ones a recompute never touches; `POST /products/categories/{category_id}/rules/preview` dry-runs this exact document before it is stored. Conditions address the `common` bucket of a product's values — a value held per locale or per channel has no single answer for a rule to test.
     * @param {string} params.rulesComputedAt - When the rule last ran TO COMPLETION and its memberships were synced. Null means no pass has ever finished — a recompute is chunked, so a half-finished pass leaves this untouched.
     * @param {object} params.values - Whatever this catalog keeps on a category beyond the model — the keys belong to the tenant, not to this app, and nothing here reads them.
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     */
    productsCategoriesUpdate(params: { id: string, code?: string, labels?: object, parentId?: string, xpath?: string, position?: number, ruleMatch?: CategoriesRuleMatch, rules?: object, rulesComputedAt?: string, values?: object }): Promise<Models.Error>;
    /**
     * Updates one category by id. A partial patch: the body names only the columns to change and every column it leaves out keeps its current value, so there is no read-modify-write and no way to blank a field by forgetting it.
     * 
     * One node of the category tree. `parent_id` is the structure this app navigates — null is a root — while `path` is kept only for importers that carry one and nothing here reads or writes it. A category is hand-picked or RULE-DRIVEN: a non-null `rules` selector makes every matching product a `product_categories` row with source `rule`, alongside the hand-picked ones, and `rules_computed_at` says when that last completed.
     * 
     * A body that names nothing writable is refused with 400 rather than answered as a no-op, an id nobody carries answers 404, and a value that collides on `code` answers 409.
     *
     * @param {string} id - The `categories` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/categories`. An id no categorie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
     * @param {string} code - The category's stable identifier — what an import and a storefront join on, and what survives a rename of the label. Unique per tenant.
     * @param {object} labels - The category name a person sees, per language tag. The catalog reads by name, not by code — a locale left blank falls back to the next filled one.
     * @param {string} parentId - The category this one hangs under. Null is a root of the tree. Deleting a parent lifts its children to the root rather than deleting them, so a mis-click never takes a subtree with it.
     * @param {string} xpath - A materialized position in the tree, kept for importers that carry one (`tools/power_tools/cordless_drills`). Nothing in this app writes or reads it — `parent_id` is the structure this app navigates.
     * @param {number} position - Order among the siblings under the same parent, ascending.
     * @param {CategoriesRuleMatch} ruleMatch - How the conditions combine: 'all' ANDs them (the default), 'any' ORs them. It is a column of its own rather than a key of `rules` because the compiler reads the two separately.
     * @param {object} rules - The selector that makes this a RULE-DRIVEN category. Null means hand-picked. Matching products are MATERIALIZED as `product_categories` rows with source `rule`, next to the hand-picked ones a recompute never touches; `POST /products/categories/{category_id}/rules/preview` dry-runs this exact document before it is stored. Conditions address the `common` bucket of a product's values — a value held per locale or per channel has no single answer for a rule to test.
     * @param {string} rulesComputedAt - When the rule last ran TO COMPLETION and its memberships were synced. Null means no pass has ever finished — a recompute is chunked, so a half-finished pass leaves this untouched.
     * @param {object} values - Whatever this catalog keeps on a category beyond the model — the keys belong to the tenant, not to this app, and nothing here reads them.
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     * @deprecated Use the object parameter style method for a better developer experience.
     */
    productsCategoriesUpdate(id: string, code?: string, labels?: object, parentId?: string, xpath?: string, position?: number, ruleMatch?: CategoriesRuleMatch, rules?: object, rulesComputedAt?: string, values?: object): Promise<Models.Error>;
    productsCategoriesUpdate(
        paramsOrFirst: { id: string, code?: string, labels?: object, parentId?: string, xpath?: string, position?: number, ruleMatch?: CategoriesRuleMatch, rules?: object, rulesComputedAt?: string, values?: object } | string,
        ...rest: [(string)?, (object)?, (string)?, (string)?, (number)?, (CategoriesRuleMatch)?, (object)?, (string)?, (object)?]    
    ): Promise<Models.Error> {
        let params: { id: string, code?: string, labels?: object, parentId?: string, xpath?: string, position?: number, ruleMatch?: CategoriesRuleMatch, rules?: object, rulesComputedAt?: string, values?: object };
        
        if ((paramsOrFirst && typeof paramsOrFirst === 'object' && !Array.isArray(paramsOrFirst))) {
            params = (paramsOrFirst || {}) as { id: string, code?: string, labels?: object, parentId?: string, xpath?: string, position?: number, ruleMatch?: CategoriesRuleMatch, rules?: object, rulesComputedAt?: string, values?: object };
        } else {
            params = {
                id: paramsOrFirst as string,
                code: rest[0] as string,
                labels: rest[1] as object,
                parentId: rest[2] as string,
                xpath: rest[3] as string,
                position: rest[4] as number,
                ruleMatch: rest[5] as CategoriesRuleMatch,
                rules: rest[6] as object,
                rulesComputedAt: rest[7] as string,
                values: rest[8] as object            
            };
        }
        
        const id = params.id;
        const code = params.code;
        const labels = params.labels;
        const parentId = params.parentId;
        const xpath = params.xpath;
        const position = params.position;
        const ruleMatch = params.ruleMatch;
        const rules = params.rules;
        const rulesComputedAt = params.rulesComputedAt;
        const values = params.values;

        if (typeof id === 'undefined') {
            throw new RevenexxException('Missing required parameter: "id"');
        }

        const apiPath = '/v1/products/categories/{id}'.replace('{id}', id);
        const apiPayload: Payload = {};
        if (typeof code !== 'undefined') {
            apiPayload['code'] = code;
        }
        if (typeof labels !== 'undefined') {
            apiPayload['labels'] = labels;
        }
        if (typeof parentId !== 'undefined') {
            apiPayload['parent_id'] = parentId;
        }
        if (typeof xpath !== 'undefined') {
            apiPayload['path'] = xpath;
        }
        if (typeof position !== 'undefined') {
            apiPayload['position'] = position;
        }
        if (typeof ruleMatch !== 'undefined') {
            apiPayload['rule_match'] = ruleMatch;
        }
        if (typeof rules !== 'undefined') {
            apiPayload['rules'] = rules;
        }
        if (typeof rulesComputedAt !== 'undefined') {
            apiPayload['rules_computed_at'] = rulesComputedAt;
        }
        if (typeof values !== 'undefined') {
            apiPayload['values'] = values;
        }
        const uri = new URL(this.client.config.endpoint + apiPath);

        const apiHeaders: { [header: string]: string } = {
            'content-type': 'application/json',
        }

        return this.client.call(
            'put',
            uri,
            apiHeaders,
            apiPayload,
        );
    }

    /**
     * One membership: this product is filed in this category. `source` says how it got there — `manual` is hand-picked, `rule` was materialized by a category rule — and the two never touch each other: a recompute only ever inserts and deletes `rule` rows, so a hand-picked membership survives every pass. `POST /products/{id}/categories` is the friendlier way to create one, because it takes the product from the path and answers with the category code and the SKU.
     * 
     * Every column of `product_categories` is an exact-match query parameter, `order` sorts by one column, and `limit`/`offset` page through `page.total`. A query key that is NOT a column is dropped rather than refused, and the `filter` object echoes the ones that were understood — that echo is the only way to tell an unfiltered answer from an empty one. It reads rows exactly as they are stored: no join is resolved, no jsonb value is unpacked.
     * 
     * Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.
     *
     * @param {number} params.limit - Page size (default 50, max 200). A larger value is clamped rather than refused.
     * @param {number} params.offset - Row offset for pagination (default 0). Page with `page.total` and `page.hasMore`.
     * @param {string} params.order - Sort by one column: 'column' | 'column.asc' | 'column.desc'. A bare column sorts ascending. Anything else is refused with 400.
     * @param {string} params.id - Exact match on `id`. The row's own id, generated by the database. A client never sends one; it reads one back and uses it in the path of every later call.
     * @param {string} params.productId - Exact match on `product_id`. The product filed into the category. Deleting the product deletes the membership with it.
     * @param {string} params.categoryId - Exact match on `category_id`. The category it is filed into. One row per (product, category), whichever way it got there.
     * @param {number} params.position - Exact match on `position`. Sort order of this product inside the category.
     * @param {Source} params.source - Exact match on `source`. How the membership came about: 'manual' is hand-picked, 'rule' was materialized by a category rule. The two never touch each other — a recompute only ever inserts and deletes `rule` rows, so a hand-picked membership survives every pass.
     * @param {string} params.createdAt - Exact match on `created_at`. When the row was created. Server-set — it is not part of any request body.
     * @throws {RevenexxException}
     * @returns {Promise<{}>}
     */
    productsProductCategoriesList(params?: { limit?: number, offset?: number, order?: string, id?: string, productId?: string, categoryId?: string, position?: number, source?: Source, createdAt?: string }): Promise<{}>;
    /**
     * One membership: this product is filed in this category. `source` says how it got there — `manual` is hand-picked, `rule` was materialized by a category rule — and the two never touch each other: a recompute only ever inserts and deletes `rule` rows, so a hand-picked membership survives every pass. `POST /products/{id}/categories` is the friendlier way to create one, because it takes the product from the path and answers with the category code and the SKU.
     * 
     * Every column of `product_categories` is an exact-match query parameter, `order` sorts by one column, and `limit`/`offset` page through `page.total`. A query key that is NOT a column is dropped rather than refused, and the `filter` object echoes the ones that were understood — that echo is the only way to tell an unfiltered answer from an empty one. It reads rows exactly as they are stored: no join is resolved, no jsonb value is unpacked.
     * 
     * Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.
     *
     * @param {number} limit - Page size (default 50, max 200). A larger value is clamped rather than refused.
     * @param {number} offset - Row offset for pagination (default 0). Page with `page.total` and `page.hasMore`.
     * @param {string} order - Sort by one column: 'column' | 'column.asc' | 'column.desc'. A bare column sorts ascending. Anything else is refused with 400.
     * @param {string} id - Exact match on `id`. The row's own id, generated by the database. A client never sends one; it reads one back and uses it in the path of every later call.
     * @param {string} productId - Exact match on `product_id`. The product filed into the category. Deleting the product deletes the membership with it.
     * @param {string} categoryId - Exact match on `category_id`. The category it is filed into. One row per (product, category), whichever way it got there.
     * @param {number} position - Exact match on `position`. Sort order of this product inside the category.
     * @param {Source} source - Exact match on `source`. How the membership came about: 'manual' is hand-picked, 'rule' was materialized by a category rule. The two never touch each other — a recompute only ever inserts and deletes `rule` rows, so a hand-picked membership survives every pass.
     * @param {string} createdAt - Exact match on `created_at`. When the row was created. Server-set — it is not part of any request body.
     * @throws {RevenexxException}
     * @returns {Promise<{}>}
     * @deprecated Use the object parameter style method for a better developer experience.
     */
    productsProductCategoriesList(limit?: number, offset?: number, order?: string, id?: string, productId?: string, categoryId?: string, position?: number, source?: Source, createdAt?: string): Promise<{}>;
    productsProductCategoriesList(
        paramsOrFirst?: { limit?: number, offset?: number, order?: string, id?: string, productId?: string, categoryId?: string, position?: number, source?: Source, createdAt?: string } | number,
        ...rest: [(number)?, (string)?, (string)?, (string)?, (string)?, (number)?, (Source)?, (string)?]    
    ): Promise<{}> {
        let params: { limit?: number, offset?: number, order?: string, id?: string, productId?: string, categoryId?: string, position?: number, source?: Source, createdAt?: string };
        
        if (!paramsOrFirst || (paramsOrFirst && typeof paramsOrFirst === 'object' && !Array.isArray(paramsOrFirst))) {
            params = (paramsOrFirst || {}) as { limit?: number, offset?: number, order?: string, id?: string, productId?: string, categoryId?: string, position?: number, source?: Source, createdAt?: string };
        } else {
            params = {
                limit: paramsOrFirst as number,
                offset: rest[0] as number,
                order: rest[1] as string,
                id: rest[2] as string,
                productId: rest[3] as string,
                categoryId: rest[4] as string,
                position: rest[5] as number,
                source: rest[6] as Source,
                createdAt: rest[7] as string            
            };
        }
        
        const limit = params.limit;
        const offset = params.offset;
        const order = params.order;
        const id = params.id;
        const productId = params.productId;
        const categoryId = params.categoryId;
        const position = params.position;
        const source = params.source;
        const createdAt = params.createdAt;


        const apiPath = '/v1/products/product_categories';
        const apiPayload: Payload = {};
        if (typeof limit !== 'undefined') {
            apiPayload['limit'] = limit;
        }
        if (typeof offset !== 'undefined') {
            apiPayload['offset'] = offset;
        }
        if (typeof order !== 'undefined') {
            apiPayload['order'] = order;
        }
        if (typeof id !== 'undefined') {
            apiPayload['id'] = id;
        }
        if (typeof productId !== 'undefined') {
            apiPayload['product_id'] = productId;
        }
        if (typeof categoryId !== 'undefined') {
            apiPayload['category_id'] = categoryId;
        }
        if (typeof position !== 'undefined') {
            apiPayload['position'] = position;
        }
        if (typeof source !== 'undefined') {
            apiPayload['source'] = source;
        }
        if (typeof createdAt !== 'undefined') {
            apiPayload['created_at'] = createdAt;
        }
        const uri = new URL(this.client.config.endpoint + apiPath);

        const apiHeaders: { [header: string]: string } = {
        }

        return this.client.call(
            'get',
            uri,
            apiHeaders,
            apiPayload,
        );
    }

    /**
     * Creates one product category membership and answers 201 with the stored row, including the id and the timestamps the database filled in — a client never sends an id, it reads one back and uses it in the path of every later call.
     * 
     * One membership: this product is filed in this category. `source` says how it got there — `manual` is hand-picked, `rule` was materialized by a category rule — and the two never touch each other: a recompute only ever inserts and deletes `rule` rows, so a hand-picked membership survives every pass. `POST /products/{id}/categories` is the friendlier way to create one, because it takes the product from the path and answers with the category code and the SKU.
     * 
     * `product_id` and `category_id` are the only columns the database refuses the row without; everything else has a default or is nullable. A second row with the same `product_id` and `category_id` answers 409.
     *
     * @param {string} params.categoryId - The category it is filed into. One row per (product, category), whichever way it got there.
     * @param {string} params.productId - The product filed into the category. Deleting the product deletes the membership with it.
     * @param {number} params.position - Sort order of this product inside the category.
     * @param {ProductCategoriesSource} params.source - How the membership came about: 'manual' is hand-picked, 'rule' was materialized by a category rule. The two never touch each other — a recompute only ever inserts and deletes `rule` rows, so a hand-picked membership survives every pass.
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     */
    productsProductCategoriesCreate(params: { categoryId: string, productId: string, position?: number, source?: ProductCategoriesSource }): Promise<Models.Error>;
    /**
     * Creates one product category membership and answers 201 with the stored row, including the id and the timestamps the database filled in — a client never sends an id, it reads one back and uses it in the path of every later call.
     * 
     * One membership: this product is filed in this category. `source` says how it got there — `manual` is hand-picked, `rule` was materialized by a category rule — and the two never touch each other: a recompute only ever inserts and deletes `rule` rows, so a hand-picked membership survives every pass. `POST /products/{id}/categories` is the friendlier way to create one, because it takes the product from the path and answers with the category code and the SKU.
     * 
     * `product_id` and `category_id` are the only columns the database refuses the row without; everything else has a default or is nullable. A second row with the same `product_id` and `category_id` answers 409.
     *
     * @param {string} categoryId - The category it is filed into. One row per (product, category), whichever way it got there.
     * @param {string} productId - The product filed into the category. Deleting the product deletes the membership with it.
     * @param {number} position - Sort order of this product inside the category.
     * @param {ProductCategoriesSource} source - How the membership came about: 'manual' is hand-picked, 'rule' was materialized by a category rule. The two never touch each other — a recompute only ever inserts and deletes `rule` rows, so a hand-picked membership survives every pass.
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     * @deprecated Use the object parameter style method for a better developer experience.
     */
    productsProductCategoriesCreate(categoryId: string, productId: string, position?: number, source?: ProductCategoriesSource): Promise<Models.Error>;
    productsProductCategoriesCreate(
        paramsOrFirst: { categoryId: string, productId: string, position?: number, source?: ProductCategoriesSource } | string,
        ...rest: [(string)?, (number)?, (ProductCategoriesSource)?]    
    ): Promise<Models.Error> {
        let params: { categoryId: string, productId: string, position?: number, source?: ProductCategoriesSource };
        
        if ((paramsOrFirst && typeof paramsOrFirst === 'object' && !Array.isArray(paramsOrFirst))) {
            params = (paramsOrFirst || {}) as { categoryId: string, productId: string, position?: number, source?: ProductCategoriesSource };
        } else {
            params = {
                categoryId: paramsOrFirst as string,
                productId: rest[0] as string,
                position: rest[1] as number,
                source: rest[2] as ProductCategoriesSource            
            };
        }
        
        const categoryId = params.categoryId;
        const productId = params.productId;
        const position = params.position;
        const source = params.source;

        if (typeof categoryId === 'undefined') {
            throw new RevenexxException('Missing required parameter: "categoryId"');
        }
        if (typeof productId === 'undefined') {
            throw new RevenexxException('Missing required parameter: "productId"');
        }

        const apiPath = '/v1/products/product_categories';
        const apiPayload: Payload = {};
        if (typeof categoryId !== 'undefined') {
            apiPayload['category_id'] = categoryId;
        }
        if (typeof position !== 'undefined') {
            apiPayload['position'] = position;
        }
        if (typeof productId !== 'undefined') {
            apiPayload['product_id'] = productId;
        }
        if (typeof source !== 'undefined') {
            apiPayload['source'] = source;
        }
        const uri = new URL(this.client.config.endpoint + apiPath);

        const apiHeaders: { [header: string]: string } = {
            'content-type': 'application/json',
        }

        return this.client.call(
            'post',
            uri,
            apiHeaders,
            apiPayload,
        );
    }

    /**
     * Deletes one product category membership by id. It is a hard delete — the row is gone, and the answer is a confirmation rather than a result to branch on.
     * 
     * Nothing in this schema references it, so nothing else changes.
     * 
     * An id no product category membership of this tenant carries answers 404; there is no 409, because every foreign key pointing at this entity resolves itself on delete rather than blocking one.
     *
     * @param {string} params.id - The `product_categories` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/product_categories`. An id no product categorie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     */
    productsProductCategoriesDelete(params: { id: string }): Promise<Models.Error>;
    /**
     * Deletes one product category membership by id. It is a hard delete — the row is gone, and the answer is a confirmation rather than a result to branch on.
     * 
     * Nothing in this schema references it, so nothing else changes.
     * 
     * An id no product category membership of this tenant carries answers 404; there is no 409, because every foreign key pointing at this entity resolves itself on delete rather than blocking one.
     *
     * @param {string} id - The `product_categories` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/product_categories`. An id no product categorie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     * @deprecated Use the object parameter style method for a better developer experience.
     */
    productsProductCategoriesDelete(id: string): Promise<Models.Error>;
    productsProductCategoriesDelete(
        paramsOrFirst: { id: string } | string    
    ): Promise<Models.Error> {
        let params: { id: string };
        
        if ((paramsOrFirst && typeof paramsOrFirst === 'object' && !Array.isArray(paramsOrFirst))) {
            params = (paramsOrFirst || {}) as { id: string };
        } else {
            params = {
                id: paramsOrFirst as string            
            };
        }
        
        const id = params.id;

        if (typeof id === 'undefined') {
            throw new RevenexxException('Missing required parameter: "id"');
        }

        const apiPath = '/v1/products/product_categories/{id}'.replace('{id}', id);
        const apiPayload: Payload = {};
        const uri = new URL(this.client.config.endpoint + apiPath);

        const apiHeaders: { [header: string]: string } = {
        }

        return this.client.call(
            'delete',
            uri,
            apiHeaders,
            apiPayload,
        );
    }

    /**
     * Reads one product category membership by its id — the whole row, every column, as it is stored.
     * 
     * One membership: this product is filed in this category. `source` says how it got there — `manual` is hand-picked, `rule` was materialized by a category rule — and the two never touch each other: a recompute only ever inserts and deletes `rule` rows, so a hand-picked membership survives every pass. `POST /products/{id}/categories` is the friendlier way to create one, because it takes the product from the path and answers with the category code and the SKU.
     * 
     * An id no product category membership of this tenant carries answers 404, and so does one belonging to another tenant: row-level security makes that row invisible rather than forbidden. A malformed id answers 400 before the route is reached.
     * 
     * Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.
     *
     * @param {string} params.id - The `product_categories` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/product_categories`. An id no product categorie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     */
    productsProductCategoriesGet(params: { id: string }): Promise<Models.Error>;
    /**
     * Reads one product category membership by its id — the whole row, every column, as it is stored.
     * 
     * One membership: this product is filed in this category. `source` says how it got there — `manual` is hand-picked, `rule` was materialized by a category rule — and the two never touch each other: a recompute only ever inserts and deletes `rule` rows, so a hand-picked membership survives every pass. `POST /products/{id}/categories` is the friendlier way to create one, because it takes the product from the path and answers with the category code and the SKU.
     * 
     * An id no product category membership of this tenant carries answers 404, and so does one belonging to another tenant: row-level security makes that row invisible rather than forbidden. A malformed id answers 400 before the route is reached.
     * 
     * Answered from the gateway's tenant cache for up to 30 minutes and dropped the moment this entity is written, because the data model changes weekly at most and every product page asks the same question.
     *
     * @param {string} id - The `product_categories` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/product_categories`. An id no product categorie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     * @deprecated Use the object parameter style method for a better developer experience.
     */
    productsProductCategoriesGet(id: string): Promise<Models.Error>;
    productsProductCategoriesGet(
        paramsOrFirst: { id: string } | string    
    ): Promise<Models.Error> {
        let params: { id: string };
        
        if ((paramsOrFirst && typeof paramsOrFirst === 'object' && !Array.isArray(paramsOrFirst))) {
            params = (paramsOrFirst || {}) as { id: string };
        } else {
            params = {
                id: paramsOrFirst as string            
            };
        }
        
        const id = params.id;

        if (typeof id === 'undefined') {
            throw new RevenexxException('Missing required parameter: "id"');
        }

        const apiPath = '/v1/products/product_categories/{id}'.replace('{id}', id);
        const apiPayload: Payload = {};
        const uri = new URL(this.client.config.endpoint + apiPath);

        const apiHeaders: { [header: string]: string } = {
        }

        return this.client.call(
            'get',
            uri,
            apiHeaders,
            apiPayload,
        );
    }

    /**
     * Updates one product category membership by id. A partial patch: the body names only the columns to change and every column it leaves out keeps its current value, so there is no read-modify-write and no way to blank a field by forgetting it.
     * 
     * One membership: this product is filed in this category. `source` says how it got there — `manual` is hand-picked, `rule` was materialized by a category rule — and the two never touch each other: a recompute only ever inserts and deletes `rule` rows, so a hand-picked membership survives every pass. `POST /products/{id}/categories` is the friendlier way to create one, because it takes the product from the path and answers with the category code and the SKU.
     * 
     * A body that names nothing writable is refused with 400 rather than answered as a no-op, an id nobody carries answers 404, and a value that collides on `product_id` and `category_id` answers 409.
     *
     * @param {string} params.id - The `product_categories` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/product_categories`. An id no product categorie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
     * @param {string} params.categoryId - The category it is filed into. One row per (product, category), whichever way it got there.
     * @param {number} params.position - Sort order of this product inside the category.
     * @param {string} params.productId - The product filed into the category. Deleting the product deletes the membership with it.
     * @param {ProductCategoriesSource} params.source - How the membership came about: 'manual' is hand-picked, 'rule' was materialized by a category rule. The two never touch each other — a recompute only ever inserts and deletes `rule` rows, so a hand-picked membership survives every pass.
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     */
    productsProductCategoriesUpdate(params: { id: string, categoryId?: string, position?: number, productId?: string, source?: ProductCategoriesSource }): Promise<Models.Error>;
    /**
     * Updates one product category membership by id. A partial patch: the body names only the columns to change and every column it leaves out keeps its current value, so there is no read-modify-write and no way to blank a field by forgetting it.
     * 
     * One membership: this product is filed in this category. `source` says how it got there — `manual` is hand-picked, `rule` was materialized by a category rule — and the two never touch each other: a recompute only ever inserts and deletes `rule` rows, so a hand-picked membership survives every pass. `POST /products/{id}/categories` is the friendlier way to create one, because it takes the product from the path and answers with the category code and the SKU.
     * 
     * A body that names nothing writable is refused with 400 rather than answered as a no-op, an id nobody carries answers 404, and a value that collides on `product_id` and `category_id` answers 409.
     *
     * @param {string} id - The `product_categories` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products/product_categories`. An id no product categorie of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
     * @param {string} categoryId - The category it is filed into. One row per (product, category), whichever way it got there.
     * @param {number} position - Sort order of this product inside the category.
     * @param {string} productId - The product filed into the category. Deleting the product deletes the membership with it.
     * @param {ProductCategoriesSource} source - How the membership came about: 'manual' is hand-picked, 'rule' was materialized by a category rule. The two never touch each other — a recompute only ever inserts and deletes `rule` rows, so a hand-picked membership survives every pass.
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     * @deprecated Use the object parameter style method for a better developer experience.
     */
    productsProductCategoriesUpdate(id: string, categoryId?: string, position?: number, productId?: string, source?: ProductCategoriesSource): Promise<Models.Error>;
    productsProductCategoriesUpdate(
        paramsOrFirst: { id: string, categoryId?: string, position?: number, productId?: string, source?: ProductCategoriesSource } | string,
        ...rest: [(string)?, (number)?, (string)?, (ProductCategoriesSource)?]    
    ): Promise<Models.Error> {
        let params: { id: string, categoryId?: string, position?: number, productId?: string, source?: ProductCategoriesSource };
        
        if ((paramsOrFirst && typeof paramsOrFirst === 'object' && !Array.isArray(paramsOrFirst))) {
            params = (paramsOrFirst || {}) as { id: string, categoryId?: string, position?: number, productId?: string, source?: ProductCategoriesSource };
        } else {
            params = {
                id: paramsOrFirst as string,
                categoryId: rest[0] as string,
                position: rest[1] as number,
                productId: rest[2] as string,
                source: rest[3] as ProductCategoriesSource            
            };
        }
        
        const id = params.id;
        const categoryId = params.categoryId;
        const position = params.position;
        const productId = params.productId;
        const source = params.source;

        if (typeof id === 'undefined') {
            throw new RevenexxException('Missing required parameter: "id"');
        }

        const apiPath = '/v1/products/product_categories/{id}'.replace('{id}', id);
        const apiPayload: Payload = {};
        if (typeof categoryId !== 'undefined') {
            apiPayload['category_id'] = categoryId;
        }
        if (typeof position !== 'undefined') {
            apiPayload['position'] = position;
        }
        if (typeof productId !== 'undefined') {
            apiPayload['product_id'] = productId;
        }
        if (typeof source !== 'undefined') {
            apiPayload['source'] = source;
        }
        const uri = new URL(this.client.config.endpoint + apiPath);

        const apiHeaders: { [header: string]: string } = {
            'content-type': 'application/json',
        }

        return this.client.call(
            'put',
            uri,
            apiHeaders,
            apiPayload,
        );
    }

    /**
     * Files one product into one category by hand, and the membership is always `source: 'manual'` — a rule recompute never deletes or shadows it. product_categories holds 28 758 rows and had no write surface that named the product it was filing. This takes the product from the route and the category from the body, which is what a bulk 'add the selected products to …' needs. The membership is always source='manual', so a rule recompute never deletes or shadows it.
     *
     * @param {string} params.id - The `products` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products`. An id no product of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
     * @param {string} params.categoryId - The category to file the product into.
     * @param {number} params.position - Sort order inside the category. Default 0.
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     */
    productsCategoriesAssign(params: { id: string, categoryId: string, position?: number }): Promise<Models.Error>;
    /**
     * Files one product into one category by hand, and the membership is always `source: 'manual'` — a rule recompute never deletes or shadows it. product_categories holds 28 758 rows and had no write surface that named the product it was filing. This takes the product from the route and the category from the body, which is what a bulk 'add the selected products to …' needs. The membership is always source='manual', so a rule recompute never deletes or shadows it.
     *
     * @param {string} id - The `products` row to address, by id. It names a row THIS TENANT holds, so no example is published — a uuid this app invented would document a call that answers 404, and a real one would be another tenant's data. Read one from `GET /v1/products`. An id no product of this tenant carries answers 404; a malformed one answers 400 before the route is reached.
     * @param {string} categoryId - The category to file the product into.
     * @param {number} position - Sort order inside the category. Default 0.
     * @throws {RevenexxException}
     * @returns {Promise<Models.Error>}
     * @deprecated Use the object parameter style method for a better developer experience.
     */
    productsCategoriesAssign(id: string, categoryId: string, position?: number): Promise<Models.Error>;
    productsCategoriesAssign(
        paramsOrFirst: { id: string, categoryId: string, position?: number } | string,
        ...rest: [(string)?, (number)?]    
    ): Promise<Models.Error> {
        let params: { id: string, categoryId: string, position?: number };
        
        if ((paramsOrFirst && typeof paramsOrFirst === 'object' && !Array.isArray(paramsOrFirst))) {
            params = (paramsOrFirst || {}) as { id: string, categoryId: string, position?: number };
        } else {
            params = {
                id: paramsOrFirst as string,
                categoryId: rest[0] as string,
                position: rest[1] as number            
            };
        }
        
        const id = params.id;
        const categoryId = params.categoryId;
        const position = params.position;

        if (typeof id === 'undefined') {
            throw new RevenexxException('Missing required parameter: "id"');
        }
        if (typeof categoryId === 'undefined') {
            throw new RevenexxException('Missing required parameter: "categoryId"');
        }

        const apiPath = '/v1/products/{id}/categories'.replace('{id}', id);
        const apiPayload: Payload = {};
        if (typeof categoryId !== 'undefined') {
            apiPayload['category_id'] = categoryId;
        }
        if (typeof position !== 'undefined') {
            apiPayload['position'] = position;
        }
        const uri = new URL(this.client.config.endpoint + apiPath);

        const apiHeaders: { [header: string]: string } = {
            'content-type': 'application/json',
        }

        return this.client.call(
            'post',
            uri,
            apiHeaders,
            apiPayload,
        );
    }
}
